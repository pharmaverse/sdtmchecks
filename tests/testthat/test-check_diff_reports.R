

# See sdtmchecks::diff_reports  documentation

test_that("diff_reports correctly shows no differences when old and new reports are identical",{
  #res = diff_reports(old_report = old, new_reports =  new) 
  expect_true(TRUE)
})

# and many more tests ...

